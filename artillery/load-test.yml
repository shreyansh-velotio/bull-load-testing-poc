config:
  target: 'http://localhost:3000'
  phases:
    - duration: 60
      arrivalRate: 10
  defaults:
    headers:
      Content-Type: 'application/json'
scenarios:
  - name: 'Add local job'
    flow:
      - post:
          url: '/local-jobs'
          json:
            jobType: 'local-base'
